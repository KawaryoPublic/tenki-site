export interface DateInfo {
    date: string;
    club: boolean;
    plan: string;
}

export interface Passwords {
    student: string | null;
    parent: string | null;
    executive: string | null;
}